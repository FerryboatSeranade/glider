# Glider Control Plane Deployment Status

This file tracks the current central-admin plus node deployment state used during the control-plane migration.

## Current Runtime

- Central admin host: `ovh-xboard`
- Central deploy directory: `/root/data/docker_data/glider-admin`
- Node host: `zgo`
- Node deploy directory: `/root/data/docker_data/glider`
- Runtime image tag under validation: `ghcr.io/ferryboatseranade/glider:v2026.06.08-control56`

The tag above is now published to GHCR and both remote compose files pull it from the registry. The earlier `docker save | ssh docker load` bootstrap path has been retired for the current hosts, and `pull_policy: never` was removed from both remote compose files.

The tag was published by GitHub Actions run `27106664483`, which completed successfully on 2026-06-08. Remote `docker manifest inspect ghcr.io/ferryboatseranade/glider:v2026.06.08-control56` shows a multi-architecture OCI index.

The control-plane PR was merged into `master` as `d78ae8037ca0dc541590d6cbf211ccd405e7736e`. The follow-up `master` push workflow run `27113588882` completed successfully on 2026-06-08 and published `ghcr.io/ferryboatseranade/glider:master`. On 2026-06-12, `ovh-xboard` verified that `ghcr.io/ferryboatseranade/glider:master` resolves to a multi-architecture OCI index. The current remote deployments intentionally remain pinned to `v2026.06.08-control56` until a deliberate rollout changes their compose files.

## Registry Publishing

The GitHub Actions build workflow is configured to publish `ghcr.io/<owner>/glider` on `master`, `main`, `dev`, and tag pushes with `packages: write`. It includes `type=ref,event=tag`, so a Git tag such as `v2026.06.08-control56` publishes the same GHCR image tag instead of only semver-stripped or `sha-*` aliases.

For a one-off local push, log in with a GitHub token that has package write permission, then push the tag:

```bash
docker login ghcr.io
docker push ghcr.io/ferryboatseranade/glider:v2026.06.08-control56
```

The current hosts already pull `v2026.06.08-control56` from GHCR. Keep `pull_policy: never` out of normal compose files so future image refreshes use the registry.

## Verified Checks

- `ovh-xboard` runs `glider-admin` on `:8444` with image `v2026.06.08-control56`.
- `zgo` runs node mode with image `v2026.06.08-control56` and publishes proxy ports `443` and `8443`; it still does not publish Admin `8444`.
- The amd64 `/usr/local/bin/glider` binary SHA256 from the GHCR image is `66223918dc645c40d30aa15db51439f57df01bc459178fbfdac2a8585de9591e` on both remote containers.
- On 2026-06-12, `ovh-xboard` still reported `glider-admin` running image `ghcr.io/ferryboatseranade/glider:v2026.06.08-control56` with binary SHA256 `66223918dc645c40d30aa15db51439f57df01bc459178fbfdac2a8585de9591e`; `/api/settings/cloudflare` returned `configured=false`, and `/api/nodes` showed `zgo` heartbeat, traffic counters, config version `d4aa2e1bd01a91de...`, and `auth_mode=shared`.
- On 2026-06-12, `zgo` still reported container `glider` running image `ghcr.io/ferryboatseranade/glider:v2026.06.08-control56` with the same binary SHA256, listening on `443` and `8443` only. No `8444` listener was present.
- On 2026-06-08, both remote containers were rechecked after `docker compose pull` and reported image `ghcr.io/ferryboatseranade/glider:v2026.06.08-control56`, running state, and the expected binary SHA above. `zgo` listened only on `443` and `8443`, and `8444` was not exposed.
- Admin exposes `/api/config/status` and shows central config version plus node synced/stale state in the Overview and Nodes views.
- `zgo` was verified synced to central config version `d4aa2e1bd01a91de...`.
- Node API now supports optional dedicated per-node tokens. Admin stores only SHA-256 token hashes; nodes without dedicated tokens continue to use the shared `GLIDER_NODE_TOKEN` for compatibility. Heartbeats record `auth_mode` so the Nodes view can show whether the last successful heartbeat used shared or dedicated auth. Dedicated-token switching was verified with `zgo` by setting a temporary dedicated token, confirming heartbeat/config sync, clearing it, and returning the node to the shared token. On `control24`, `zgo` reports `auth_mode=shared`.
- ACME DNS-01 issuance now waits for `_acme-challenge` TXT propagation before accepting the challenge. Tune with `GLIDER_ACME_DNS_PROPAGATION_TIMEOUT` and `GLIDER_ACME_DNS_POLL_INTERVAL`.
- Admin now has a server-side rules health worker. It runs rule IP-info probes on `GLIDER_RULES_HEALTH_INTERVAL`, stores the latest snapshot in MongoDB, and exposes it through `GET /api/rules/health?latest=1`. Manual health check persistence was verified on `ovh-xboard`: 15 rule checks saved, with 14 ok and 1 error at the time of validation.
- Domain saves update only editable domain and Cloudflare config fields; they do not overwrite stored certificates, Cloudflare record IDs, sync timestamps, or last-error runtime state.
- Domains support manual fullchain/private-key import in addition to ACME issuance. Import validates the key pair, reads certificate expiry, stores a new certificate version, and reuses the node certificate sync API.
- Manual certificate import now verifies that the leaf certificate covers the domain being imported before saving it. SAN names are authoritative when present; CN fallback is accepted only for CN-only certificates; wildcard certificates match only one label. This prevents importing a wrong-domain certificate and syncing it to nodes. It is covered by `TestImportedDomainCertificateValidatesDomainCoverage` and deployed in `control49`.
- Manual certificate import now rejects leaf certificates that are expired or not valid yet. Node certificate file writes perform the same validity-window check before touching local certificate files, so a stale or corrupted central snapshot cannot overwrite the last usable local certificate. This is covered by `TestImportedDomainCertificateRejectsInvalidValidityWindow` and `TestWriteCertificateFilesRejectsExpiredCertificateWithoutOverwritingExistingFiles`, and deployed in `control51`. On `control51`, a temporary `codex-expired-cert.example.com` import returned `400 certificate expired...`, saved only `last_error` with no `cert_version`, and the temporary domain was deleted.
- The central node certificate snapshot API now skips certificates that are missing `expires_at` or already expired, so nodes do not need to pull known-unusable certificate material from Admin. Domain runtime and failover readiness also require assigned nodes to report an unexpired matching domain certificate version; an expired node-side cert version is shown as not synced and is not a failover candidate. This is covered by `TestCertificateSnapshotForNodeOnlyIncludesAssignedEnabledCertificates` and `TestDomainRuntimeAndFailoverRequireUnexpiredNodeCertificate`, and deployed in `control54`. On `control54`, `GET /api/node/certs?node_id=zgo` returned the empty snapshot version with no domains, matching the current empty domain list.
- Node startup cache loading now treats certificate cache errors as certificate-sync errors instead of config-cache errors. If `config_snapshot.json` is valid but the cached certificate snapshot contains expired or otherwise invalid certificate material, the node still applies the cached config and starts the proxy, records `certErr`, and waits for the next central cert sync to repair certificates. This is covered by `TestNodeSyncerLoadCacheIgnoresInvalidCertificateCache` and deployed in `control55`.
- Domain list/detail API responses redact certificate PEM and private-key material; the node certificate sync API is the only API that returns full certificate files.
- Manual certificate import was end-to-end tested with `codex-cert-sync-test.example.com`: Admin stored the certificate, `zgo` pulled cert version `8ba9e097492f6ad1...`, wrote `fullchain.pem` and `privkey.pem`, and reported the cert version in heartbeat.
- `zgo` now has the node-template SNI listener enabled with `listen=tls://:443?certDir=/etc/glider-certs,http://` plus the existing `listen=:8443`. A temporary `codex-sni-test.example.com` certificate was imported through Admin, `zgo` pulled cert version `469c3214dcf29458...`, Admin reported `cert synced 1/1`, and `curl --proxy-insecure --proxy https://user1:pass1@codex-sni-test.example.com:443 --resolve codex-sni-test.example.com:443:127.0.0.1 https://ipinfo.io/json` succeeded from the node. The temporary domain was deleted afterward and the node removed the local certificate directory on the next sync.
- Node heartbeats now include per-domain certificate state under `cert_domains`; the Domains view uses it to show `cert synced <n>/<total>` for assigned nodes. On `control24`, `zgo` reports the empty certificate snapshot version `e3b0c44298fc1c14...` with `cert_domains_count=0` and `cert_error` is empty.
- The Domains view now also shows certificate days remaining and renewal state (`renew due`, `renew expired`, or `renew in <n>d`) based on each domain's `renew_before_days` window.
- Domains API responses now include a read-only `runtime` object with structured DNS, certificate, renewal, and per-node certificate sync state for scripting and UI reuse. This was verified on `control15` with a temporary `codex-runtime-test.invalid` domain assigned to `zgo`: `runtime.status=active`, `dns_status=dns pending`, `cert_status=cert missing`, `cert_renew_status=missing`, `cert_sync_status=cert not issued`, and assigned node state showed `zgo` online with public IP `38.49.59.207`; the temporary domain was deleted after verification.
- Cloudflare DNS sync now preserves the previous record metadata and last successful sync timestamp when a DNS sync attempt fails, while still saving the latest error. This protects failover state from being erased by a transient Cloudflare API outage; it is covered by `TestSyncCloudflareDNSFailurePreservesPreviousRecordState` and is deployed in `control16`.
- Cloudflare DNS sync success is now covered by `TestSyncCloudflareDNSCreatesRecordAndStoresMetadata` and `TestSyncCloudflareDNSUpdatesExistingIPv6Record`. These tests verify automatic zone lookup, existing-record lookup, A-record creation, existing AAAA-record update through `PUT`, IPv6 default record type selection, bearer-token auth, default TTL, proxied flag propagation, and persisted zone/record metadata. This is a test-only hardening change after `control34`; no new runtime image was required.
- ACME DNS-01 challenge cleanup now skips empty Cloudflare record IDs before calling the deleter, while still attempting every non-empty cleanup and ignoring individual deletion failures. This avoids meaningless delete calls if a partially constructed challenge list contains blanks. It is covered by `TestCleanupCloudflareRecordsDeletesEveryRecordAndIgnoresErrors` and deployed in `control35`.
- Domain Cloudflare record type now supports an Admin UI `Auto` option. Auto is stored as an empty `cloudflare.record_type`, so DNS sync chooses A for IPv4 node public IPs and AAAA for IPv6 node public IPs instead of forcing new domains to A. This is covered by `TestDomainConfigSetPreservesAutoRecordType`, `TestAdminHTMLSmoke`, and the IPv6 DNS sync tests, and is deployed in `control36`.
- Domain saves now reject unsupported Cloudflare record types and negative TTL values before writing to MongoDB. The only accepted record types are `Auto` (stored as empty), `A`, and `AAAA`; TTL `0` still normalizes to Cloudflare automatic TTL `1`. This is covered by `TestDomainConfigSetRejectsInvalidCloudflareRecordConfig` and deployed in `control37`.
- Domain saves now normalize editable Cloudflare DNS metadata before writing to MongoDB: `zone_id` and `zone_name` are trimmed, `record_name` is validated as a domain and normalized to lowercase without a trailing dot, and invalid record names are rejected. This prevents direct API writes from later causing mismatched Cloudflare record lookups. This is covered by `TestDomainConfigSetPreservesRuntimeState` and `TestDomainConfigSetRejectsInvalidCloudflareRecordConfig` and deployed in `control43`.
- Cloudflare token verification can now optionally test a target zone. `POST /api/settings/cloudflare/verify` accepts `domain` and `dns_edit_test`; with a domain it verifies zone lookup, and with `dns_edit_test=true` it creates then deletes a temporary `_glider-check-*` TXT record to prove DNS edit permission. The Admin UI exposes this as `Zone test domain` and `DNS edit test`. This is covered by `TestCloudflareCheckZoneReadOnly`, `TestCloudflareCheckZoneDNSEditCreatesAndDeletesTXT`, and `TestAdminHTMLSmoke`, and is deployed in `control44`.
- Domain DNS sync now has a preview path. `POST /api/domains/<domain>/dns-plan` returns the selected node, target public IP, Cloudflare zone, record name/type, TTL/proxied state, existing record content when available, and planned action (`create`, `update`, or `unchanged`). The Admin Domain Editor exposes this as `Preview DNS`, and successful `Sync DNS` responses include the same plan for auditability. This is covered by `TestPlanCloudflareDNSShowsCreateTarget`, `TestPlanCloudflareDNSShowsExistingUpdate`, and `TestAdminHTMLSmoke`, and is deployed in `control45`.
- Manual DNS preview/sync now validates the target node before contacting Cloudflare. The target must be assigned to the domain, have a fresh healthy heartbeat, and if the domain already has a certificate version, the node must have reported that same domain certificate version. Domain saves also reject an `active_node_id` that is not in `node_ids`, and clear active node when no nodes are assigned. This aligns manual DNS changes with certificate-aware failover safety. It is covered by `TestDomainConfigSetValidatesActiveNodeAssignment` and `TestValidateDomainDNSTargetRequiresAssignedHealthyCertSyncedNode`, and deployed in `control50`. On `control50`, a temporary `codex-dns-guard.example.com` domain assigned to `ghost-node` rejected both `dns-plan` and `sync-dns` to `zgo` with `node zgo is not assigned...` before Cloudflare was contacted, then the domain was deleted.
- Cloudflare DNS preview/sync now rejects node target IPs that are not public routable addresses, including loopback, private, link-local, carrier-grade NAT, documentation/test networks, multicast, and ULA/documentation IPv6 ranges. The plan uses the normalized public IP string for A/AAAA content. This is covered by `TestPlanCloudflareDNSRejectsNonPublicTargetIPWithoutCallingCloudflare` and the existing A/AAAA plan/sync tests. The check was added at the Cloudflare plan layer in `control52` and moved into the Admin DNS target guard in `control53`, so bad node IPs are rejected before Cloudflare token initialization. On `control53`, a temporary `codex-public-ip-guard.example.com` assigned to a test node with `public_ip=192.0.2.10` returned `400 node codex-private-ip-node public_ip is not a public IP address` even though Cloudflare settings are not configured, then the temporary domain and node were deleted.
- Domain certificate issuance now has a preview path. `POST /api/domains/<domain>/cert-plan` verifies Cloudflare zone lookup and returns the ACME email/directory, `_acme-challenge` record, current certificate version/expiry, days remaining, renewal window, assigned nodes, and planned action (`issue`, `renew`, or `not_due`) without contacting ACME or creating DNS records. The Admin Domain Editor exposes this as `Preview Cert`, and successful `Issue Cert` responses include the same preflight plan. This is covered by `TestBuildCertificatePlanShowsIssueAndRenewState`, `TestRenewBeforeDaysDefault`, and `TestAdminHTMLSmoke`, and is deployed in `control46`.
- Manual `Issue Cert` now reuses the `zone_id` selected by the certificate preflight plan when creating ACME DNS-01 TXT records. This avoids a second zone lookup and keeps the zone displayed in `Preview Cert` aligned with the zone used during issuance. The fallback order is explicit preflight zone, stored domain zone, then Cloudflare zone lookup. This is covered by `TestResolveCertificateZoneIDUsesOverrideThenStoredThenLookup` and deployed in `control47`.
- Automatic certificate renewal now uses the same certificate-plan helper as manual issue/preview, including ACME email/directory defaults and Cloudflare zone lookup. Renewal then passes the planned zone ID into DNS-01 TXT creation, so scheduled renewals and manual issuance use the same zone-selection behavior. This is covered by `TestCertificatePlanWithSettingsResolvesZoneAndDefaults` and deployed in `control48`.
- Cloudflare settings persistence now has direct test coverage for trimming non-secret fields, encrypting new API tokens before MongoDB writes, preserving an existing token when the form submits a blank token, and unsetting only the token when the Admin user clicks clear. This is covered by `TestCloudflareSettingsUpdateEncryptsAndPreservesToken` and deployed in `control38`.
- The Cloudflare Settings UI now labels the Account ID field as account-token-only and shows `token owner: account token` or `token owner: user token` based on whether Account ID is set. This makes the distinction between account-owned and user-owned Cloudflare API tokens visible in the Admin panel. It is covered by `TestAdminHTMLSmoke` and deployed in `control56`.
- ACME DNS-01 challenge record names are now generated through a tested helper. It normalizes case/trailing dots and maps wildcard certificates such as `*.example.com` to `_acme-challenge.example.com`, which prevents malformed TXT record names during certificate issuance. This is covered by `TestDNS01RecordNameNormalizesDomain` and deployed in `control39`.
- `GLIDER_ADMIN_ALLOWED_CIDRS` now provides an optional process-level source IP/CIDR allowlist for Web Admin and Admin APIs, plus `GLIDER_ADMIN_TRUST_PROXY_HEADERS` for trusted reverse-proxy deployments. Node sync APIs remain governed by node tokens and are not blocked by the Admin allowlist. This is covered by `TestAdminSourceAllowed` and `TestAdminSourceAllowedTrustsProxyHeadersOnlyWhenEnabled` and deployed in `control17`. The current `ovh-xboard` deployment leaves the allowlist empty to preserve existing reachability.
- Tests now explicitly verify failed config apply keeps the previous `rules.d` files and node sync does not overwrite `cache/config_snapshot.json` with a config that failed to apply. These checks are covered by `TestConfigApplierKeepsOldConfigWhenReloadFails` and `TestNodeSyncerDoesNotCacheFailedApply` and are included in the `control18` source package.
- Node certificate sync now skips certificate file/cache writes when the central `cert_version` is unchanged and the local cert snapshot plus certificate files match central state, including the empty certificate snapshot case. If a cert file is missing or differs from the snapshot, the next sync restores it. This reduces needless cache churn without losing self-healing behavior, is covered by `TestNodeSyncerAppliesCertificateSnapshot` and `TestNodeSyncerAppliesEmptyCertificateSnapshot`, and is deployed in `control24`.
- Node certificate snapshots now have a pure filtering test that verifies `/api/node/certs` material only includes enabled domains assigned to the requesting node and skips disabled, unassigned, or incomplete certificate records. This is covered by `TestCertificateSnapshotForNodeOnlyIncludesAssignedEnabledCertificates` and is deployed in `control24`.
- Certificate issuance, import, and renewal failures now share a tested failure helper that preserves the last usable certificate version, fullchain, private key, and expiry while recording `last_error`. This prevents a transient ACME or import failure from causing nodes to lose an existing certificate. It is covered by `TestCertificateFailurePreservesLastUsableCertificate` and deployed in `control24`.
- Domain failover target selection is now isolated behind `failoverTarget` and tested with healthy-active, stale-active, disabled-domain, disabled-failover, and unhealthy-candidate cases. This protects the DNS failover worker from switching away from a healthy active node. It is covered by `TestFailoverTargetOnlySwitchesWhenActiveIsUnhealthy` and deployed in `control24`.
- Domain failover target selection is now certificate-sync aware. If a domain already has a certificate version, Admin will only switch DNS to a healthy assigned node whose heartbeat reports the same domain certificate version; domains without an issued certificate keep the previous heartbeat-only behavior. This prevents DNS failover from moving a TLS domain to a node that has not received the certificate yet. It is covered by `TestFailoverSkipsHealthyNodeWithoutSyncedDomainCertificate` and deployed in `control40`.
- Domain runtime responses and the Domains UI now expose failover readiness: `failover_status`, `failover_ready_nodes`, optional `failover_blocked_reason`, and per-assigned-node `failover_ready`. This makes it visible when DNS failover is blocked because no healthy assigned node has the current domain certificate. This is covered by `TestDomainRuntimeReportsFailoverReadiness` and `TestAdminHTMLSmoke` and deployed in `control41`.
- `failover_ready_nodes` now counts standby takeover candidates only and excludes the current active node. Per-node `failover_ready` still shows whether each assigned node has the needed heartbeat/certificate state. This keeps the summary aligned with the actual DNS failover target set. It is covered by the active-node case in `TestDomainRuntimeReportsFailoverReadiness` and deployed in `control42`.
- Node heartbeat now separates certificate sync errors into `cert_error` instead of polluting the general node `error` field. This lets Admin show config sync as healthy even if `/api/node/certs` has a transient failure. It is covered by `TestNodeSyncerReportsCertSyncErrorSeparately` and deployed in `control24`.
- TLS server certificate hot reload is now covered by `TestLoadCertificateIfChangedReloadsUpdatedFiles`: unchanged cert/key files reuse the cached certificate, while changed files are loaded on the next certificate lookup. This supports node-side certificate renewal without restarting the container and is included in the `control25` source package.
- TLS server listeners now support `certDir=/etc/glider-certs` for SNI-based certificate selection. A node can serve multiple assigned domains from the certificate sync directory, with wildcard fallback such as `*.example.com`; static `cert`/`key` remains supported as a fallback. This is covered by `TestGetCertificateUsesCertDirByServerName`, `TestGetCertificateFallsBackToStaticCertificate`, and `TestGetCertificateUsesWildcardCertDirCandidate`, and is deployed in `control26`.
- TLS server construction now has direct tests for the public URL entry point: `certDir` alone is accepted, while incomplete static certificate arguments (`cert` without `key`, or `key` without `cert`) are rejected. This is covered by `TestNewTLSServerAcceptsCertDirWithoutStaticCertificate` and `TestNewTLSServerRejectsIncompleteStaticCertificate`, and is included in the `control27` source package.
- SNI certificate selection through `certDir` is now covered by a real TLS handshake test using `net.Pipe`. `TestCertDirSelectionDuringTLSHandshake` verifies that a client handshake with `ServerName=beta.example.com` receives the `beta.example.com` certificate from the synced certificate directory, not merely through a direct `getCertificate` helper call. This is included in the `control28` source package.
- TLS certificate reload now keeps the last usable certificate if a refreshed cert/key is temporarily missing or invalid. This prevents a broken intermediate certificate write from making existing TLS listeners fail new handshakes after they already loaded a good certificate. This is covered by `TestLoadCertificateIfChangedKeepsPreviousCertificateOnReloadFailure`, `TestLoadCertificateIfChangedKeepsPreviousCertificateWhenKeyTemporarilyMissing`, and `TestGetCertificateKeepsPreviousSNINameCertificateOnReloadFailure`, and is deployed in `control29`.
- TLS SNI certificate selection now falls back to a configured static `cert`/`key` when a matching `certDir` domain directory exists but cannot be loaded on first use. This keeps an optional fallback certificate useful for broken per-domain material instead of failing the handshake immediately. This is covered by `TestGetCertificateFallsBackToStaticCertificateWhenSNIInitialLoadFails` and is deployed in `control30`.
- ACME DNS-01 challenge cleanup is isolated behind `cleanupCloudflareRecords`. The cleanup path attempts every non-empty created `_acme-challenge` TXT deletion and ignores individual cleanup failures so they do not replace the primary issuance result. The helper was introduced in `control31`, and empty record IDs are skipped starting in `control35`.
- Node certificate file writes now validate every fullchain/private-key pair with `tls.X509KeyPair` before writing local cert files. The validation happens for the whole batch before any local certificate file is touched, so one invalid domain in a multi-domain snapshot cannot partially overwrite another valid local domain. Invalid central cert snapshots fail cert sync, report `cert_error`, and do not overwrite previous local certificate files. This is covered by `TestWriteCertificateFilesRejectsInvalidPairWithoutOverwritingExistingFiles`, `TestWriteCertificateFilesValidatesBatchBeforeWritingAnyFiles`, and `TestNodeSyncerRejectsInvalidCertificateSnapshotWithoutOverwritingFiles`, and is deployed in `control33`.
- Node certificate file writes also verify that each leaf certificate covers the assigned domain before touching local cert files. This guards nodes from stale or manually corrupted central certificate snapshots. It is covered by `TestWriteCertificateFilesRejectsWrongDomainCertificate` and deployed in `control49`.
- Node config sync now supports `ETag` / `If-None-Match` based on `config_version`. When a node already has the current config, `/api/node/config` returns `304 Not Modified` and does not resend users, passwords, or rule contents. This is covered by `TestNodeSyncerUsesConditionalConfigRequestForCurrentVersion` and deployed in `control34`.
- Node certificate sync now supports `ETag` / `If-None-Match` based on `cert_version`. Nodes send a conditional cert request only after confirming the local cert snapshot and files are complete; if a file is missing or differs, the node pulls the full snapshot to repair itself. This is covered by `TestNodeSyncerUsesConditionalCertificateRequestOnlyWhenLocalSnapshotComplete` and deployed in `control34`.
- On `control34`, `ovh-xboard` was verified returning `304 Not Modified` with an empty body for both `/api/node/config?node_id=zgo` and `/api/node/certs?node_id=zgo` when the matching ETag was supplied.
- Deleting the test domain was also tested: `zgo` pulled the empty cert snapshot and removed the local test certificate directory.
- Admin `/api/nodes` receives heartbeat from `zgo` with public IP `38.49.59.207`, config version, cert version, and traffic snapshot fields. On `control56`, `zgo` reports config version `d4aa2e1bd01a91de...`, empty cert snapshot version `e3b0c44298fc1c14...`, and `auth_mode=shared`.
- On `control56`, `ovh-xboard` `/api/domains` currently returns an empty domain list because no real Cloudflare-backed domain has been configured yet. The Admin HTML served from the container includes the failover readiness UI hooks, Cloudflare zone/DNS edit verification controls, DNS preview controls, certificate preview controls, and Cloudflare account-token guidance.
- The Admin UI contains `Domains -> Cloudflare Settings`, `Zone test domain`, `DNS edit test`, `Preview DNS`, and `Preview Cert`.
- The Admin UI contains `Verify Token`, node deletion, domain search, and traffic summary UI hooks.
- Proxy checks from `zgo` through both `127.0.0.1:8443` and the TLS/SNI `443` listener to `https://ipinfo.io/json` succeed and return exit IP `91.230.73.88` (`AS50131 Spartan Host Ltd`, Dallas, Texas, US).
- `zgo` has no Admin listener on `8444`.

## Remaining External Inputs

- Configure a Cloudflare API Token and ACME email in the Admin UI before DNS sync and certificate issuance can be tested.
- The current `ovh-xboard` `.env` has `GLIDER_SETTINGS_KEY` set, but Cloudflare API token, account ID, and ACME email are empty. On `control56`, `/api/settings/cloudflare` returns `configured=false`, so the Admin UI correctly reports Cloudflare settings as not configured.
- The recommended Cloudflare token scope is the specific zone with `Zone:Read` and `DNS:Edit`.
- Use the Admin UI `Verify Token` action after saving the token. Enter `account_id` for Cloudflare account-owned tokens, enter a `Zone test domain` for zone lookup, and enable `DNS edit test` when you want to prove write/delete permission with a temporary TXT record.
- `deploy/scripts/domain_onboarding_check.py` can validate a real domain after Cloudflare settings are saved. Its default mode checks Admin auth, node heartbeat, Cloudflare zone read, saved domain existence, DNS preview, and certificate preview. It only saves Cloudflare settings, saves a domain assignment, changes Cloudflare DNS, or issues a certificate when run with `--save-cloudflare-settings`, `--save-domain`, `--sync-dns`, or `--issue-cert`. When saving Cloudflare settings, existing account ID and ACME fields are preserved unless a replacement value or explicit clear flag is supplied.
- The onboarding helper behavior is covered by `deploy/scripts/domain_onboarding_check_test.py`, and GitHub Actions runs those Python unittests alongside `go test ./...`.

## Current Provisioning Rollout

- `ovh-xboard` is running Admin image `ghcr.io/ferryboatseranade/glider:v2026.06.13-provision4` from `/root/data/docker_data/glider-admin`.
- `zgo` is registered in the Admin `servers` collection with `server_id=zgo`, `node_id=zgo`, host `38.49.59.207`, deploy directory `/root/data/docker_data/glider`, image `ghcr.io/ferryboatseranade/glider:v2026.06.13-provision4`, and redacted private-key credentials.
- The Admin-side SSH test job `job-kG4nGtsnzpZ07hQK` succeeded from `ovh-xboard` to `zgo` using the stored server credentials.
- The Admin-side preflight job `job-twvPYHTPqsFvdUUy` succeeded from `ovh-xboard` to `zgo`, reporting Docker `29.1.3`, Docker Compose `2.40.3`, deploy directory and compose file present, container `glider` running, `19G` free disk, `1234MB` available memory, and ports `443`/`8443` listening.
- The Admin-side upgrade job `job-ybmMWnZ3Ve9A5Z3S` succeeded and upgraded `zgo` to image `ghcr.io/ferryboatseranade/glider:v2026.06.13-provision4`.
- After the upgrade, `zgo` reported container `glider` running, compose image `ghcr.io/ferryboatseranade/glider:v2026.06.13-provision4`, published ports `443` and `8443`, and no Admin `8444` listener.
- A proxy check through `zgo` on `127.0.0.1:8443` to `https://ipinfo.io/json` returned exit IP `91.230.73.88` (`AS50131 Spartan Host Ltd`, Dallas, Texas, US), confirming the data plane stayed functional after the Admin-driven upgrade.
- Admin `/api/servers` now reports `zgo` status `upgraded`, `has_private_key=true`, and `last_deploy_job=job-ybmMWnZ3Ve9A5Z3S`.
- Admin `/api/nodes` reports `zgo` heartbeat with public IP `38.49.59.207`, config version `d4aa2e1bd01a91de...`, cert version `e3b0c44298fc1c14...`, and no config or certificate sync error.
- Server `auth_type` now accepts private-key aliases such as `key`, `private-key`, `privatekey`, `ssh_key`, and `ssh-key`, normalizing them to `private_key` for API automation. This is covered by `TestServerAuthTypeAliases`.
- The current branch adds a non-destructive server preflight job at `POST /api/servers/<server_id>/preflight-node`. It checks SSH reachability, Docker/Compose availability, deploy directory and compose file presence, current `glider` container state, disk/memory summary, and selected proxy port listeners before running a deploy or upgrade. It is covered by `TestHostPortFromMapping` and `TestAdminHTMLSmoke`, and deployed in `v2026.06.13-provision4`.
