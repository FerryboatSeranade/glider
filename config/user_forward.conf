# Glider config with user-specific forwarding rules

# Listen on port 8444 with multiple user authentication
listen=socks5://:8444?users=user1:pass1,user2:pass2,user3:pass3

# User-specific rule files
rules-dir=user.rules.d

# Verbose logging for debugging
verbose=true
